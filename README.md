# Graph Detectives Streamlit Activity

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

The app uses Streamlit's built-in LaTeX rendering (`st.latex` and Markdown math) so mathematical symbols display clearly.
